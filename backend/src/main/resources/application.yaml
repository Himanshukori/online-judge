spring:
  application:
    name: online-judge
  datasource:
    url: jdbc:postgresql://localhost:5432/online_judge
    username: postgres
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  

server:
  port: 8080
jwt:
  secret: Chaange_me_in_production